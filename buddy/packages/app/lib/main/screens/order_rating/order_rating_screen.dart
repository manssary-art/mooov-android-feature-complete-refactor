import 'package:flutter/material.dart';

class OrderRatingScreen extends StatelessWidget {
  const OrderRatingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("OrderRatingScreen"),
      ),
    );
  }
}
